// hooker preloader

#include <stdio.h>
#include <limits.h>
#include <stdlib.h>
#include <unistd.h>

int main() {
    /* //-----prototype-----
    realpath(path_to_so, real_path_to_so);
    setenv("LD_PRELOAD", "libhookv.so", 1);
    execvp(argv[1], &argv[1]);
    */
    return 0;
}
